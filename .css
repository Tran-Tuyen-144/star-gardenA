body {
  font-family: 'Segoe UI', Arial, sans-serif;
  background: linear-gradient(135deg, #f6f7fb, #e2e8f0);
  padding: 18px;
  min-height: 100vh;
}

.box {
  max-width: 760px;
  margin: 18px auto;
  background: #fff;
  padding: 24px;
  border-radius: 12px;
  box-shadow: 0 8px 24px rgba(20, 20, 40, 0.1);
}

h1 {
  margin: 0 0 10px;
  font-size: 24px;
  color: #6b5bff;
}

p {
  margin: 6px 0 12px;
  color: #444;
  font-size: 14px;
}

input[type=text] {
  width: 100%;
  padding: 12px;
  border-radius: 8px;
  border: 1px solid #ddd;
  font-size: 14px;
}

.buttons {
  margin: 12px 0;
}

button {
  padding: 10px 16px;
  border-radius: 8px;
  border: none;
  background: #6b5bff;
  color: #fff;
  font-weight: 600;
  cursor: pointer;
  margin-right: 8px;
  transition: all 0.2s ease;
}

button:disabled {
  background: #ccc;
  cursor: not-allowed;
}

button:hover:not(:disabled) {
  background: #5848d2;
}

#canvas {
  display: block;
  margin: 12px 0;
  padding: 12px;
  border-radius: 10px;
  background: #fdfdfd;
  border: 1px solid #eee;
  text-align: center;
}

.note {
  font-size: 13px;
  color: #666;
  margin-top: 10px;
}
